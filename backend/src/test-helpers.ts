// Note: This file is deprecated since we moved to in-memory store
// Tests should now use the store directly instead of SQLite
// Keeping this file for backward compatibility but it's no longer used

export function createTestDb() {
  // Return a mock object for backward compatibility
  // Tests should be updated to use store directly
  return null;
}

